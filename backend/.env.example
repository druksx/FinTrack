# Database Configuration
DATABASE_URL="postgresql://druksx@localhost:5432/fintrack?schema=public"

# Server Configuration
PORT=3333
NODE_ENV=development

# CORS Configuration (optional, as it's hardcoded in main.ts for now)
# FRONTEND_URL=http://localhost:3000

# JWT Configuration (coming soon with auth)
# JWT_SECRET=your-jwt-secret
# JWT_EXPIRATION=1d

# OAuth Configuration (coming soon)
# GITHUB_ID=your-github-oauth-id
# GITHUB_SECRET=your-github-oauth-secret
# GOOGLE_ID=your-google-oauth-id
# GOOGLE_SECRET=your-google-oauth-secret